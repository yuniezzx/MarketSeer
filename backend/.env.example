# Flask 配置
FLASK_DEBUG=True
SECRET_KEY=dev-secret-key-change-in-production

# 服务器配置
FLASK_HOST=0.0.0.0
FLASK_PORT=5001

# ============================================================
# 数据库配置
# ============================================================
# 
# 运行方式:
# - 默认使用 SQLite: python run.py
# - 显式指定 SQLite: python run.py --db sqlite
# - 使用 Supabase: python run.py --db supabase (需要配置下面的 SUPABASE_DATABASE_URI)
#
# ============================================================

# Supabase PostgreSQL 数据库连接 URI
# -----------------------------------------------------------
# 使用 Supabase 数据库时需要配置此变量
# 
# 获取步骤:
# 1. 登录 Supabase Dashboard: https://app.supabase.com/
# 2. 选择你的项目
# 3. 点击左侧 "Project Settings" > "Database"
# 4. 在 "Connection string" 部分选择 "Session" 模式
# 5. 复制连接字符串并替换下面的占位符:
#    - [project-ref]: 你的项目引用 ID (如: abcdefghijklmnop)
#    - [password]: 你在创建项目时设置的数据库密码
#    - [region]: 你的项目区域 (如: ap-southeast-1, us-east-1)
#
# 连接字符串格式 (Session pooler - 端口 6543):
# SUPABASE_DATABASE_URI=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
#
# 示例 (请替换为你的实际值):
# SUPABASE_DATABASE_URI=postgresql://postgres.abcdefghijklmnop:your_password_here@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# SQLite 数据库配置
# -----------------------------------------------------------
# SQLite 是默认数据库,无需额外配置
# 数据库文件将自动创建在: backend/data/marketseer.db

# 数据源配置 (可选，默认启用 akshare 和 efinance)
# DATA_SOURCES_AKSHARE_ENABLED=True
# DATA_SOURCES_AKSHARE_PRIORITY=1
# DATA_SOURCES_EFINANCE_ENABLED=True
# DATA_SOURCES_EFINANCE_PRIORITY=2
# DATA_SOURCES_YFINANCE_ENABLED=False
# DATA_SOURCES_YFINANCE_PRIORITY=3

# 数据更新配置 (可选，默认 3600 秒)
# UPDATE_INTERVAL=3600

# 日志配置 (可选)
# LOG_LEVEL=INFO

# CORS 配置 (可选，默认允许 localhost:3000 和 localhost:5173)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
