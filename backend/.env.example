# ============================================================
# ENV 配置
# 配置使用 load_dotenv() 和 os.environ.get 读取,确保系统环境变量优先级 > .env 文件优先级
# ============================================================
ENV=development # development, production

# ============================================================
# Flask 配置
# ============================================================
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# 定时器任务配置
SCHEDULER_API_ENABLED=True
SCHEDULER_TIMEZONE=Asia/Shanghai
SCHEDULER_JOB_DEFAULTS={"coalesce": false, "max_instances": 1}

# logging 配置
LOG_LEVEL=INFO

# ============================================================
# 数据库配置
# ============================================================

# SQLite
SQLite_DB_URI=sqlite:///./data/sqlite.db
# SUPABASE
# Replace [project-ref], [password], and [region] with actual values
# SUPABASE_URI=postgresql://postgres.abcdefghijklmnop:your_password_here@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# AkShare 原始数据保存配置 (可选，默认启用)
# -----------------------------------------------------------
# 文件保存位置: backend/data/akshare_raw/
# 文件命名格式: {api_name}_{symbol}_{timestamp}.json
SAVE_RAW_DATA=false

# AkShare 原始数据保存保留天数 (可选，默认30天)
SAVE_RAW_DATA_DAYS=30

# ============================================================
# CORS 跨域配置（开发环境）
# ============================================================
# 允许的来源列表（逗号分隔,不要有空格）
# 
# 使用场景说明：
# 
# 1. 本地开发（前后端在同一台电脑）：
#    CORS_ORIGINS=http://localhost:5173,http://localhost:3000
#
# 2. 远程开发（前端在不同 WiFi,使用 ngrok）：
#    步骤：
#    a) 启动后端：python run.py
#    b) 启动 ngrok：ngrok http 5000
#    c) 复制 ngrok 提供的 HTTPS URL（例如 https://abc123.ngrok-free.app）
#    d) 将该 URL 添加到下面的配置中：
#       CORS_ORIGINS=http://localhost:5173,https://abc123.ngrok-free.app
#    
#    注意：ngrok 免费版每次启动 URL 会变化,所以保持 ngrok 运行避免频繁更新配置
#
# 默认配置（本地开发）：
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://regardable-subastringent-francisco.ngrok-free.dev

