🧠 MarketSeer 项目开发文档（第二版）
一、项目概述

MarketSeer 是一个金融数据采集、整合与分析系统，旨在通过多数据源（AkShare、eFinance、yFinance 等）自动获取股票市场数据，构建统一的数据中心，为前端可视化与分析提供支持。

系统采用：

后端：Flask + SQLite

前端：React (JavaScript)

项目管理工具：uv (Python 环境管理)

数据库操作：SQLAlchemy ORM

运行环境：本地开发 → 测试 → 部署生产

二、项目结构
MarketSeer/
│
├── backend/ # 后端核心目录
│ ├── app/
│ │ ├── **init**.py
│ │ ├── routes/ # 路由接口
│ │ ├── models/ # 数据库模型
│ │ ├── services/ # 数据采集与合并逻辑
│ │ ├── utils/ # 工具函数（日志、时间、格式化）
│ │ ├── config.py # 全局配置（API Keys、路径、环境变量）
│ │ └── tasks/ # 定时任务或数据更新脚本
│ ├── data/
│ │ └── marketseer.db # SQLite 数据库文件
│ ├── pyproject.toml # uv 配置文件
│ ├── uv.lock
│ ├── run.py # 后端启动入口
│ └── README.md
│
└── frontend/ # 前端项目
├── src/
│ ├── components/
│ ├── pages/
│ ├── services/ # 调用后端 API
│ ├── hooks/
│ └── utils/
├── public/
├── package.json
├── vite.config.js
└── README.md

三、数据库设计

1. 设计说明

初始阶段仅包含 stock_info 一张表，用于存储股票基础信息。
数据来源于 AkShare 与 eFinance 的接口整合结果。

未来会拓展更多表：
stock_price, stock_financials, stock_dividend, stock_analysis, 等。

2. 表结构定义
   字段名 类型 说明
   id INTEGER PRIMARY KEY AUTOINCREMENT 主键
   code TEXT 股票代码（唯一）
   name TEXT 股票名称
   market TEXT 市场（如上交所、深交所）
   industry TEXT 行业分类
   list_date TEXT 上市日期
   status TEXT 当前状态（上市 / 退市）
   source TEXT 数据来源（akshare / efinance / merged）
   update_time TEXT 最后更新时间（ISO 格式）
3. 数据库 ER 图
   +---------------------+
   | stock_info |
   +---------------------+
   | id (PK) |
   | code |
   | name |
   | market |
   | industry |
   | list_date |
   | status |
   | source |
   | update_time |
   +---------------------+

（后续可扩展：）

stock_info 1───∞ stock_price
∞───1 stock_financials

四、后端接口设计

1. 接口设计原则

RESTful 风格

所有接口以 /api/ 开头

数据返回统一格式：

{
"status": "success",
"data": {...},
"message": ""
}

2. 接口文档模板
   接口名称 股票列表查询
   请求方式 GET
   URL /api/stocks
   参数 无（可后续扩展分页、筛选）
   返回示例
   {
   "status": "success",
   "data": [
   {
   "code": "600519",
   "name": "贵州茅台",
   "market": "SH",
   "industry": "饮料制造",
   "list_date": "2001-08-27"
   }
   ]
   }

接口名称 单只股票详情查询
请求方式 GET
URL /api/stocks/<code>
参数 code: 股票代码
返回示例
{
"status": "success",
"data": {
"code": "600519",
"name": "贵州茅台",
"market": "SH",
"industry": "饮料制造",
"list_date": "2001-08-27",
"status": "上市"
}
}

接口名称 数据更新接口
请求方式 POST
URL /api/update/stocks
说明 从 AkShare、eFinance 等数据源更新数据库
返回
{
"status": "success",
"message": "stock_info updated successfully",
"updated_rows": 2345
}

五、前后端交互流程

用户在前端发起请求（通过 Axios）；

Flask 后端接收请求；

若为查询类接口 → 从 SQLite 读取；

若为更新类接口 → 调用 AkShare / eFinance → 清洗合并 → 写入数据库；

返回 JSON 响应；

前端渲染展示。
